<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <style media="screen">
      .make-smaller{
        width: 600px;
      }
    </style>
    <title></title>
  </head>
  <body>
    <%Post.feed_Organizer(Post.all).each do |post| %>
      <div class="panel panel-success make-smaller">
        <div class="panel-heading">
          <h3 class="panel-title"><a href="/users/<%= post.user.id %>"><%= post.user.user_name %></a></h3>
          <br>
          <%= Post.time_parser(post.created_at)  %>
        </div>
        <div class="panel-body">
          <%=post.post_content  %>
        </div>
        <div class="panel-body">
          <% post.tags.each do |tag| %>
          <a href="/tags/<%= tag.id %>" class="btn btn-success btn-xs"><%= tag.name %></a>
          <% end %>
        </div>
      </div>
    <%end %>
  </body>
</html>
